# Exemple d'IP locale (à adapter à ton réseau)
DEV_API_URL=http://<TON_IP_LOCALE>:3001/api
PROD_API_URL=https://api.tonapp.com/api

# WebSocket (optionnel si tu l'utilises)
WS_URL=http://<TON_IP_LOCALE>:3001/

# Clé publique Mapbox
MAPBOX_API_KEY=pk.xxxxxxxx

# Adresse IP locale (juste pour référence éventuelle)
HOST_IP=<TON_IP_LOCALE>

# Environnement par défaut (optionnel)
NODE_ENV=development
